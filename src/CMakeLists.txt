add_subdirectory(utils)
add_subdirectory(solvers)
add_subdirectory(fbga)
if(FBGA_BUILD_3D OR FBGA_BUILD_PYTHON)
  add_subdirectory(fbga3d)
endif()
if(FBGA_BUILD_PYTHON)
  add_subdirectory(python)
endif()
